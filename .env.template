# 生产环境配置模板 - 请替换所有 [需要修改] 的内容

# ===========================================
# 🚨 数据库配置 - 必须修改
# ===========================================
DB_HOST=host.docker.internal  # 如果连接失败，改为服务器内网IP
DB_PORT=3306
DB_USER=root
DB_PASSWORD=[需要修改：你的宝塔MySQL密码]

# ===========================================
# 🚨 安全配置 - 全部必须修改
# ===========================================
# JWT密钥（64位随机字符串）- 运行: openssl rand -base64 64
JWT_SECRET=[需要修改：64位随机字符串]

# 激活码密钥（32位随机字符串）- 运行: openssl rand -base64 32  
LICENSE_SECRET=[需要修改：32位随机字符串]

# Token加密密钥（32位随机字符串）- 运行: openssl rand -base64 32
TOKEN_ENCRYPTION_KEY=[需要修改：32位随机字符串]

# 管理员密钥（强密码）
ADMIN_KEY=[需要修改：管理员密钥]

# 管理员密码（强密码）
ADMIN_PASSWORD=[需要修改：管理员密码]

# Redis密码（强密码）
REDIS_PASSWORD=[需要修改：Redis密码]

# ===========================================
# 🚨 邮件服务配置 - 必须修改
# ===========================================
SMTP_HOST=smtp.163.com
SMTP_PORT=465
SMTP_USER=[需要修改：你的邮箱@163.com]
SMTP_PASS=[需要修改：你的邮箱SMTP授权码]

# ===========================================
# 🚨 域名配置 - 必须修改
# ===========================================
# 替换 your-domain.com 为你的实际域名
CLAUDE_BASE_URL=https://claude.lqqmail.xyz
CLAUDE_PROXY_DOMAIN=claude.lqqmail.xyz

# 前端API地址
FRONTEND_ACTIVATION_API_URL=https://[需要修改：你的域名]/api/activation
FRONTEND_POOL_API_URL=https://[需要修改：你的域名]/api/pool

# 管理器API地址
MANAGER_ACTIVATION_API_URL=https://[需要修改：你的域名]/api/activation
MANAGER_POOL_API_URL=https://[需要修改：你的域名]/api/pool

# ===========================================
# ✅ 以下配置可以保持默认
# ===========================================
# Redis配置
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# Token配置
TOKEN_EXPIRES_IN=86400
DB_CONNECTION_LIMIT=20
DB_ACQUIRE_TIMEOUT=60000
DB_TIMEOUT=60000

# 应用信息
VITE_APP_NAME=Claude License System
VITE_APP_VERSION=1.0.0
