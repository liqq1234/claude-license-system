# 开发环境配置 - activation-backend

# 应用配置
NODE_ENV=development
PORT=8888

# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_NAME=license_server
DB_USER=root
DB_PASS=4693090li

# Redis 配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=123456
REDIS_DB=0

# 邮件服务配置（开发环境）
SMTP_HOST=smtp.163.com
SMTP_PORT=465
SMTP_USER=liqingquan20030522@163.com
SMTP_PASS=XGikY39q8rnMZuKr

# 安全配置（开发环境使用较简单的值）
JWT_SECRET=dev-jwt-secret-key
JWT_EXPIRES_IN=7d
LICENSE_SECRET=dev-license-secret
TOKEN_ENCRYPTION_KEY=dev-token-encryption-key
ADMIN_KEY=admin123

# 外部服务配置
POOL_BACKEND_URL=http://localhost:8787
CLAUDE_PROXY_DOMAIN=claude.ai

# 清理服务配置
CLEANUP_INTERVAL=3600000
CLEANUP_EXPIRED_CODES=true
CLEANUP_EXPIRED_SESSIONS=true
CLEANUP_OLD_LOGS=true
